# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
develop-eggs/
dist/
downloads/
eggs/
.eggs/
# Python lib directories (but not ui/src/lib/)
/lib/
/lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.env.jarvis
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Pywright configuration file
pyrightconfig.json
.windsurfpyright/

# env vars for wfsm
.env
.env.*
.env*
!ai_platform_engineering/agents/litellm/mcp/.env.example

# Others
a2a-python
.superpowers/

# Local configuration files
.DS_Store

# LangGraph Server
.langgraph_api/

# helm chart
charts/*/values-secrets.yaml
charts/*/values-ingress.yaml
charts/*/tmp/*

.aider*
.aider.*

# Documentation
## Only allow svg
*.png
!ai_platform_engineering/knowledge_bases/rag/webui/src/assets/icons/*.png
!docs/screenshots/*.png
*.jpg
*.jpeg
# Multimodal regression fixtures are real sample files the harness reads at test time.
!tests/fixtures/multimodal/*.jpg
!tests/fixtures/multimodal/*.jpeg

# Keycloak realm config — local copy may contain org-specific IdP secrets.
# Use deploy/keycloak/realm-config.example.json as the starting point.
deploy/keycloak/realm-config.json

# Exclude actual secret files but keep the directory structure
deploy/data/*
!deploy/data/.gitkeep

# Milvus
volumes
volumes_old

ai_platform_engineering/knowledge_bases/rag/webui/node_modules


# VS Code
.vscode/

# Helm
*.tgz

# Neo4j
.neo4j/

# node
node_modules/
# package-lock.json
# package.json

# Next.js
.next/
out/
ui/.next/
ui/out/
next-env.d.ts
ui/next-env.d.ts

# Use case data storage (file-based)
ui/data/
data/
# Helm chart data must be tracked. Please do not add ignores on this directory!!!
!charts/ai-platform-engineering/data/

# TypeScript build cache
*.tsbuildinfo

# Cursor — track commands, rules, and hooks, ignore everything else
.cursor/*
!.cursor/commands/
!.cursor/rules/
!.cursor/hooks/
!.cursor/hooks.json

# Claude Code — track commands and skills, ignore everything else
.claude/*
!.claude/commands/
!.claude/commands/**
!.claude/skills
!.claude/skills/**

# .agents/skills symlinks to .claude/skills for ~/.agents/skills compatibility
!.agents/
!.agents/skills

# UI Core (local development)
caipe-ui-core/

# Coverage reports
coverage.json

# Helm lock files - regenerated during build
**/Chart.lock

# Temporary commit message files
.git_commit_msg.txt
*.git_commit_msg.txt

# Local CA certificate bundles (host-specific)
certs/

# Benchmark intermediate output (published version lives in docs/docs/evaluations/)
integration/benchmark_results.md

# Streaming conformance reports (generated by simulate_slack_stream.py --report)
tests/reports/

# We no longer have a separate docker-compose directory.
# Spec 102: the previous e2e overlay was inlined into docker-compose.dev.yaml
# via ${VAR:-default} substitution (see Makefile E2E_COMPOSE_ENV) so the
# e2e lane reuses the dev compose file and a curated COMPOSE_PROFILES set.
docker-compose/*

# Spec 102 — RBAC test artifacts (junit, playwright traces, html reports)
test-results/
ui/test-results/
ui/playwright-report/
tests/rbac/e2e/test-results/
tests/rbac/e2e/playwright-report/
tests/rbac/e2e/node_modules/

# Local-only seed config overrides (e.g. config/app-config.local.yaml,
# config/slack-bot-config.local.yaml). The committed *.yaml files in
# config/ stay as example templates; *.local.yaml is where each developer
# stores their populated dev seed (LLM models, MCP endpoints, etc).
# Also covers main's CAIPE_APP_CONFIG_FILE / CAIPE_SLACK_BOT_CONFIG_FILE overrides.
app-config.local.yaml
config/*.local.yaml
config/*.local.yml

# Webex recording exports
*.vtt
*.har

# Scratch files commonly created by coding agents when drafting PR descriptions.
# These should be passed to `gh pr edit --body-file` and then deleted, never committed.
PR_BODY.md
PR_BODY.txt
.pr-body.md
COMMIT_BODY.md

# GitHub App credentials (never commit)
.github-apps-credentials/
reports/

*.drawio